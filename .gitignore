# Xcode / build
build/
DerivedData/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
xcuserdata/
*.xcuserstate
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# The Xcode project is GENERATED from project.yml via XcodeGen.
# Regenerate with `xcodegen generate`. Do not commit the generated project.
Endurance.xcodeproj/

# SwiftPM
EndurancePackages/.build/
EndurancePackages/.swiftpm/
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Signing identities and secrets — NEVER commit.
# A .p12 contains a private key; committing one publishes a credential that
# then lives in git history even after deletion.
*.p12
*.p8
*.cer
*.certSigningRequest
*.mobileprovision
certificate*

# Secrets / local config (never commit)
*.xcconfig.local
Secrets.local.xcconfig
